# gui:
#   authorColors:
#     "Rogin Farrer": "#ff0000"

git:
  diffRenderers:
    - command: difft --color=always
      type: extDiff
customCommands:
  - key: "G"
    command: "gh pr view -w {{.SelectedLocalBranch.Name}} || gh pr create -w --head {{.SelectedLocalBranch.Name}}"
    context: "localBranches"
    description: "Open Github PR in browser"
  - key: "G"
    command: "gh pr view -w {{.SelectedLocalBranch.Name}} || gh pr create -w --head {{.SelectedLocalBranch.Name}}"
    context: "commits"
    description: "Open Github PR in browser"
  - key: "<c-r>"
    command: "git pull --rebase origin main"
    context: "localBranches"
    description: "Pull rebase origin/main"
